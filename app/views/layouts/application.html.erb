<!DOCTYPE html>
<html lang="en">
<head>
  <title>The Merritt Baby</title>
  <meta name="description" content="This is a small hobby web application developed by Jim Merritt for Jim & Kelly's Baby Shower and Gender Reveal.">   
  <meta name="author" content="Jim Merritt">  
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= favicon_link_tag 'favicon.ico' %>  
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <script src="//use.typekit.net/han1ref.js"></script>
  <script>try{Typekit.load({ async: true });}catch(e){}</script>  
</head>
<body>
  <%= render 'layouts/header' %>
  <div class="container">
    <% flash.each do |message_type, message| %>
      <div class="row">
        <div class="wrapper">    
          <div class="alert-<%= message_type %>"><p><%= message %></p></div>
        </div>
      </div>
    <% end %>
    <%= yield %>
    <% if Rails.env.development? %>
      <div class="row">
        <%= debug(params) %>
      </div>
    <% end %>
  </div>
</body>
</html>